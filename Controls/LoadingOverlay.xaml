<UserControl x:Class="CeraRegularize.Controls.LoadingOverlay"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:CeraRegularize.Controls"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="400">
    <UserControl.Resources>
        <Storyboard x:Key="SpinStoryboard" RepeatBehavior="Forever">
            <DoubleAnimation Storyboard.TargetName="SpinnerRotate"
                             Storyboard.TargetProperty="Angle"
                             From="0"
                             To="360"
                             Duration="0:0:1.1" />
        </Storyboard>
    </UserControl.Resources>
    <Border x:Name="Root"
            Background="{DynamicResource OverlayBackgroundBrush}"
            CornerRadius="{Binding CornerRadius, RelativeSource={RelativeSource AncestorType=local:LoadingOverlay}}"
            Visibility="Collapsed"
            IsHitTestVisible="True">
        <Grid>
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <Grid Width="48" Height="48">
                    <Ellipse Stroke="{DynamicResource OverlaySpinnerBrush}"
                             StrokeThickness="4"
                             StrokeDashArray="1.6 2.6"
                             StrokeDashCap="Round"
                             Fill="Transparent"
                             RenderTransformOrigin="0.5,0.5">
                        <Ellipse.RenderTransform>
                            <RotateTransform x:Name="SpinnerRotate" />
                        </Ellipse.RenderTransform>
                    </Ellipse>
                </Grid>
                <TextBlock x:Name="MessageText"
                           Margin="0,12,0,0"
                           FontSize="12"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource OverlayTextBrush}"
                           Text="Loading..." />
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
